# AGENTS.md

本文件记录项目的长期工程约定、文档治理规则、协作流程和安全边界。详细的阶段执行顺序、汇报模板和进度记录格式见：

~~~text
docs/开发协作与文档管理规范.md
~~~

## 1. 项目概览

项目结构约定：

- `backend/` 为 Java Spring Boot 后端；只在动态数据、持久化或其他后端业务需求确认后增加对应能力。
- `frontend/` 的正式技术基线为 Nuxt 4、Vue 3 和 TypeScript。
- 前端实现可以参考本地 Figma Make 项目，但数据链路、接口和业务规则优先级高于视觉原型。
- 项目发布前通常使用 Docker 构建，并按项目文档发布到目标服务器。

如果没有找到 Git 信息，先询问用户是否需要纳入 Git 管理。所有 Git 操作默认由用户执行，agent 不执行 Git 命令。

`AGENTS.md` 和 `docs/` 下的项目文档属于工程基线，不得通过 `.gitignore` 排除；是否提交及具体 Git 操作仍由用户负责。

## 2. 文档优先与文档职责

开始任何开发任务前，必须完整阅读以下文档：

1. docs/需求文档.md
2. docs/总体开发计划.md
3. docs/开发进度.md

文档职责：

- docs/需求文档.md：记录已确认的业务范围、页面、字段、接口、权限、状态、交互和验收要求。
- docs/总体开发计划.md：记录阶段划分、技术路线、详细设计和实施顺序。
- docs/开发进度.md：只记录实际实现、验证、用户验证、遗留问题和下一步计划。
- docs/开发协作与文档管理规范.md：记录阅读顺序、阶段执行流程、汇报模板和文档维护规则。
- AGENTS.md：记录长期有效、强制性的工程和安全约定，不记录易变化的当前阶段状态。

用户明确确认的要求优先于普通文档；最新确认文档优先于历史描述。需求、文档和现有代码发生冲突时，必须暂停并向用户说明冲突，不得自行选择方案。

如果文档缺少当前阶段所需的页面、接口、字段、状态、权限、解析策略或验证设计，必须先提出文档补充计划，默认等待用户确认后再编码。

## 3. 开始任务前的强制流程

每个开发阶段都必须按以下顺序执行：

1. 完整阅读指定文档。
2. 检查当前工作区状态、目录、现有代码、配置、测试和文档。
3. 重点确认上一阶段已完成内容和用户已调整内容没有被破坏。
4. 结合最新文档判断当前阶段、已完成范围和实际待办。
5. 在编码前向用户汇报：
   - 当前阶段和现状；
   - 已完成内容及必须保留的改动；
   - 准备检查或调整的目录；
   - 文档中的设计依据；
   - 具体实现或验证计划；
   - 验证方案；
   - 潜在冲突和风险。
6. 默认等待用户确认后再编码，或执行超出只读检查范围的修改。
7. 只实现文档已经确认的范围，不自行增加业务、页面、接口、表结构或权限。
8. 完成后只执行必要的编译、定向测试、前端类型检查和生产构建。
9. 更新 docs/开发进度.md。
10. 最终说明修改文件、验证结果和遗留问题。

完整流程和汇报格式以 docs/开发协作与文档管理规范.md 为准。

## 4. 变更边界与安全

- 不修改 Figma Make 参考项目，除非用户明确要求。
- 不修改其他项目的代码、配置或部署文件，参考项目只读。
- 不删除、重置、覆盖或回退用户已有改动。
- 不执行 git reset --hard、git checkout -- 或其他可能丢失用户改动的命令。
- 不在未获明确授权时上传文件、推送镜像、修改服务器、重启公共 Gateway、执行数据库脚本或发送外部消息。
- 只读检查可以包括文件读取、文本搜索、配置展开、静态语法检查和状态查询。
- 可能影响外部服务、数据库、其他项目或用户数据的操作，必须在计划中明确并等待确认。
- 终端命令、脚本、日志和文档中不得输出真实数据库密码、Redis 密码、ACR 凭据、管理员密码或 Cookie。
- 生产环境文件、私钥和登录凭据不得提交代码仓库或放入 Docker 镜像。

## 5. 后端通用约定

当前后端技术约定：

- Spring Boot 4.1.1。
- Java 17。
- 后端实际承载数据库业务时使用 MyBatis Plus 3.5.17 Spring Boot 4 Starter、Flyway 和 MySQL。
- Redis 和 Spring Session Redis 只在出现共享缓存、登录或分布式会话需求，并完成专项设计后启用。
- 不得为了预留能力提前创建数据库表、缓存结构、Session 或无业务用途的后端接口。

接口约定：

- Controller 默认只使用 GET、POST。
- POST 入参必须使用对象。
- GET 入参超过 3 个时，改为 POST 对象入参并向用户说明。
- 接口路径使用驼峰命名，例如 /api/user/avatarOptions。
- 禁止使用短横线或下划线作为接口路径命名。
- 对象类型的请求和响应字段必须补充中文注释。
- 后端异常目录为 common/exception。
- 后端响应目录为 common/response。
- 代码注释优先使用中文。

数据库约定：

- 数据库时间字段使用 _time 后缀，例如 create_time、update_time、fetched_time。
- 表不创建外键，由业务代码保证关系。
- 主键默认使用 BIGINT AUTO_INCREMENT。
- 不在字段名中体现默认货币单位。
- 复杂 SQL 优先放入 Mapper XML。
- 简单查询可以使用 MyBatis Plus Wrapper。

新增实体时必须同步更新：

1. Entity；
2. Mapper interface；
3. Mapper XML；
4. Repository；
5. 必要的测试和文档。

Mapper XML 必须放在：

~~~text
backend/src/main/resources/mapper/**
~~~

MyBatis Plus 必须保留：

~~~text
mapper-locations: classpath*:/mapper/**/*.xml
~~~

数据库迁移：

- Flyway 脚本放在 backend/src/main/resources/db/migration/。
- 已执行的迁移不得直接修改，必须新增迁移。
- 清理历史数据的迁移必须有明确条件，不能误删业务关系数据。
- 数据库迁移、管理员初始化和业务种子初始化的执行时机必须写入项目文档。

## 6. 配置、认证和数据安全

配置必须区分：

- application.yaml：公共配置；
- application-dev.yaml：开发环境；
- application-prod.yaml：生产环境。

配置值优先通过环境变量提供，密码、密钥和生产地址不得提供非空硬编码默认值。开发环境如需便利配置，应使用不提交仓库的本地环境文件或显式环境变量。

后续实际引入账号功能时，认证约定如下：

- 使用 Spring Session Redis、HttpOnly Cookie 和 CSRF。
- 不使用 JWT、Bearer Token 或 localStorage 保存登录凭证。
- Cookie 的 Secure、SameSite 和 Session namespace 必须按环境配置。
- Redis Session 的数据库编号、密码和 namespace 必须通过环境变量注入。
- 不把明文管理员密码写入 Flyway、镜像、日志或仓库。
- 如果未来存在管理员角色，账号创建和角色提升流程必须先写入专项设计，不得通过迁移脚本写入明文密码。

动态数据源和监控约定：

- 数据源异常不得伪装为正常业务结果，也不得把过期、缺失或请求失败的数据标记为当前事实。
- 主备数据源、解析策略、异常状态、更新时间、缓存期限和回退规则必须以对应工具的专项设计为准。
- 不得因为单次请求失败擅自写入具有长期业务含义的不可用、下线或不存在状态。
- 实际验收使用的数据样本、年份、地区和规则版本必须以最新运行配置、专项文档和用户确认口径为准。

## 7. Docker、Compose 和发布约定

镜像：

- 后端使用多阶段 Dockerfile，构建阶段使用 JDK，运行阶段只使用 JRE。
- 前端使用 Node 构建，最终使用 Nginx 提供静态文件。
- 项目根目录作为 Docker build context，并配置 .dockerignore。
- 生产构建明确指定目标平台，例如 linux/amd64。
- 镜像使用不可变版本 Tag，保留上一版本用于回滚。
- 前后端版本可以独立发布。
- 运行时镜像更换为 Alpine 前，必须验证 Java HTTPS、CA、时区、字体、本地库和数据源连接。
- 构建、推送、拉取、导入和重建必须分别验证。

网络和端口：

- 公共 Gateway 独占宿主机 80、443。
- frontend、backend 默认只使用 expose，不映射宿主机端口。
- frontend 加入 public-gateway 和项目私有网络。
- backend 加入 shared-backend 和项目私有网络。
- 容器内访问 MySQL、Redis 使用 Docker 服务名，不使用 127.0.0.1。
- 项目私有网络用于隔离不同项目的 frontend 和 backend。
- public-gateway 和 shared-backend 为外部共享网络，不能随意删除或重建。

域名和 IP 项目：

- 域名项目使用精确 server_name，并按需配置 80 跳转、443 和证书。
- IP 项目使用已有 default_server 和路径前缀。
- 新增域名配置不得覆盖已有 IP 项目的 default_server。
- IP 路径前缀部署时，Nuxt 的 `app.baseURL`、静态资源路径和 Nginx 路径前缀必须一致。
- 修改共享 Gateway 前必须执行 nginx -t，并在维护窗口观察其他项目。
- Gateway 配置文件必须确认实际使用的是 .conf，而不是 .conf.example。
- 后续工具确需 SSE 时，反向代理必须使用 HTTP/1.1、关闭代理缓冲和请求缓冲，并设置长读取超时。

发布安全：

- 服务器发布脚本可以自动更新生产镜像 Tag，但必须先备份生产环境文件。
- 镜像拉取成功后再持久化新的镜像地址，避免拉取失败时提前改变生产配置。
- 只发布单个服务时使用 --no-deps。
- 不使用 docker compose restart 代替 pull 和 --force-recreate。
- 不执行 docker compose down -v、docker system prune -a 或 docker volume prune。
- 离线镜像传输必须使用 docker save、docker load 和 SHA256 校验。
- 镜像回滚不会自动回滚 Flyway 数据库结构。

## 8. 测试和验证约定

默认不使用测试优先，不主动运行完整单测或大规模测试，除非用户明确要求或改动风险需要。

按改动范围执行必要验证：

后端：

- Maven 编译；
- 受影响模块或关键链路的定向测试；
- 必要时检查 Flyway、MySQL、Redis、Session 和 healthcheck。

前端：

- TypeScript 类型检查；
- Nuxt 生产构建和预渲染检查；
- 受影响页面的定向浏览器验证；
- 必要时检查移动端、桌面端、路由、SEO 元数据、站点地图和静态资源路径。

部署：

- Compose config --quiet；
- Dockerfile 构建和目标平台检查；
- 镜像运行时 Java、curl、Nginx 和时区检查；
- 容器状态、healthcheck、日志和网络；
- Nginx nginx -t；
- 域名项目验证 HTTP、HTTPS、证书、API、Session/CSRF 和 SSE；
- IP 项目验证路径前缀、静态资源、API 和已有项目访问。

验证结果必须区分：

- agent 自动验证；
- 用户手工验证；
- 尚未执行的验证；
- 因环境缺失而无法执行的验证。

## 9. 文档维护约定

每完成一个阶段，必须更新 docs/开发进度.md，至少记录：

- 实现结果；
- 验证结果；
- 用户验证结果；
- 未解决问题或冲突；
- 下一步计划。

不要把当前阶段、临时版本号、未确认方案或现场故障写入 AGENTS.md。长期规则写入 AGENTS.md，当前状态写入开发进度，详细过程写入协作规范或专项设计文档。

## 10. 最终交付

最终汇报必须包含：

- 修改文件；
- 保留或未修改的关键文件；
- 代码和配置实现结果；
- 执行过的验证命令及结果；
- 用户已验证内容；
- 未执行的验证及原因；
- 遗留问题、风险和下一步建议。

最终汇报不得把“计划执行”写成“已经完成”，不得隐瞒环境限制、验证缺失或未解决冲突。
